---
"@kairo-ui/theme": minor
"@kairo-ui/react": minor
---

Add `Field` — ties a label, description and error message to a single form
control and generates the matching `id`, `htmlFor` and `aria-describedby`
wiring for you. Works with every existing Kairo form component, since they all
forward their props through to the underlying control.
