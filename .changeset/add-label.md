---
"@kairo-ui/theme": minor
"@kairo-ui/react": minor
---

Add `Label` — a themed native `<label>`, the first one in the package. Pairs
with a control either explicitly through `htmlFor` or implicitly by wrapping
it; dims automatically when it wraps a disabled control, or on demand through
its own `disabled` prop.
